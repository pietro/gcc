# { dg-options "-fstropping=upper" }  #
# Operator test.  #
BEGIN OP +:= = (INT a, b) INT: a + b;
      OP +:= = (INT a, REAL b) INT: ROUND (a - b);

      ASSERT ((2+:=1) = 3);
      ASSERT ((2+:=1.0) = 1)
END
